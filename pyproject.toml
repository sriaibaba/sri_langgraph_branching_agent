[project]
name = "langgraph-branching-agent"
version = "0.1.0"
description = "LangGraph tool-calling agent for Apple 10-K (Pinecone) and weather"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.1.1",
    "pypdf>=5.0.0",
    "pinecone>=6.0.0",
    "langgraph>=0.6.0",
    "langchain>=0.3.26",
    "langchain-core>=0.3.0",
    "langchain-google-genai>=2.1.5",
]
